[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zt_langgraph"
version = "0.1.0"
description = "Python adapter module"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["zt_langgraph*"]